<%= link_to gravatar_for(current_user, size: 52), current_user %>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "내 프로필 보기", current_user %>
</span>
<span>
  <%= pluralize(current_user.microposts.count, "번 트윗을 남겼어요 ") %>
</span>